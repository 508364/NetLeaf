添加Auto-complete机制，根据自动补全charset 标签和自动引入Vue库(新功能)都是它的，但是这部分独立编译成库，可以解耦运行，只是运行时提示没有这个库部分功能不可用
自动路由端点跳转(新功能，自动在路由端点里面找相近的端点，在404页面提示：你可能在找：)和更好的错误反馈页面(新功能，允许设置自定义错误页面，但是强制要求预留输出报错信息等变量硬编码区域，可以和自动跳转联动，也就是404不生效)和上面一样：独立编译成库，可以解耦运行，只是运行时提示没有这个库部分功能不可用